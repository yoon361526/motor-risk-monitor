# .env 템플릿 (실제 키 없음 — 이 파일은 커밋해도 안전)
#
# 사용법: 이 파일을 복사해 .env 로 만들고, 아래 값에 실제 키를 넣으세요.
#   (.env 는 .gitignore 로 깃허브에 올라가지 않습니다)
#
# PowerShell:  Copy-Item .env.example .env

ANTHROPIC_API_KEY=sk-ant-여기에_실제_키

# (선택) Claude 모델 변경
# ANTHROPIC_MODEL=claude-sonnet-4-6
